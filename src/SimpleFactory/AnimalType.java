package SimpleFactory; // Project Organization

/* --- AnimalType Enum --- */
// Defines the supported animal types.
// Used by the AnimalFactory to decide which Animal to create.
public enum AnimalType {
    DOG,
    CAT
}
