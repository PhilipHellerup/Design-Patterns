package Composite; // Project Organization

/* --- FileSystemComponent Interface --- */
// Component interface.
// Declares the common operation for both
// individual files (leaves) and folders (composites).
public interface FileSystemComponent {
    // # Methods
    // Displays details of the component
    void showDetails();
}
